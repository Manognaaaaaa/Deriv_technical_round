# Optional: only needed for --mode llm. Rule mode (the default) needs no key.
GROQ_API_KEY=your-groq-api-key-here
LLM_MODEL=openai/gpt-oss-120b
# rule | llm
ANSWER_MODE=rule
# Optional overrides
# KB_PATH=kb.json
# LLM_PRICE_INPUT_PER_1M=0.15
# LLM_PRICE_OUTPUT_PER_1M=0.60
